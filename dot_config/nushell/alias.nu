alias hx = helix

alias j = just
alias jg = just --justfile ~/.global-justfile --working-directory .

# dot edit
alias dote = chezmoi edit --watch

# cd exercism wasm
alias cdew = cd ~/exercism/wasm
 
